/*
#include <iostream>
using namespace std;

int n, m;

int main() {
	int T;
	cin >> T;
	while (T--) {
		cin >> n >> m;
		int num = 'L' - 'A' + 1;
		if (n < num || m < 4)cout << -1 << endl;
		else cout << (num - 1) * m + 4 << endl;
	}
	return 0;
}
*/